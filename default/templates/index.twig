<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Mon Project Blog</title>

    <link rel="stylesheet" href="/css/index.css">

</head>

<body>

    <a href="{{ path_for('blog')}}">connection</a>

    <h1>Blog accueil</h1>



    <table class="tableau">

        {% for person in persons %}

        <tr>

            <td>
                <a href="{{path_for('personArticle', {'id':person.id})}}">{{ person.name|e }}
                </a>
            </td>
            <td>{{ person.email }}</td>

        </tr>
        {% endfor %}

    </table>



</body>

</html>